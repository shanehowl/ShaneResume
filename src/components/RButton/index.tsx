import React, {type PropsWithChildren} from 'react';
import {Text, View} from 'react-native';

export const RButton: React.FC<PropsWithChildren<{}>> = ({}) => {
  return (
    <View>
      <Text>Testing label</Text>
    </View>
  );
};
